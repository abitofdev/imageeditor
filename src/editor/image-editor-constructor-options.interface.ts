export interface IImageEditorConstructorOptions {
    /**
     * Enable or disable alpha support on the canvas. Enabling this option will consume more memory for the alpha channel.
     * Default is `false`
     */
    alpha: boolean;
}
